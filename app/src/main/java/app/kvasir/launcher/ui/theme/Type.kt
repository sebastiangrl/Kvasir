package app.kvasir.launcher.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import app.kvasir.launcher.R

/**
 * Spec 020 / RF-020-01 —
 * Anurati (Emmeran Richard) — personal use only; credit in README.
 * Used solely for the Home weekday abbreviation.
 */
val AnuratiFontFamily = FontFamily(
    Font(resId = R.font.anurati_regular, weight = FontWeight.Normal),
)
