package app.kvasir.launcher.domain.model

/**
 * Spec 016 / RF-016-01 — Settings hub sections (ephemeral navigation).
 */
enum class SettingsSection {
    Appearance,
    Pomodoro,
    Favorites,
    Habits,
    Permissions,
}
